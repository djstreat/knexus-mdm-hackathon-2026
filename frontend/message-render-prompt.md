I just added app/components/ai-elements/message. Please change the way that the dashboard at app/page.js renders the "Reports": Each row of the table should be a button, where clicking on it opens the full text of the issue in an `Alert` (from app/components), where the alert body is rendered using the stack of components `Message>MessageContent>MessageResponse>{text}` in order to leverage the Message's native capability for rendering dynamic Markdown
